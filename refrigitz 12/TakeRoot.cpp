#include "TakeRoot.h"


namespace RefrigtzDLL
{

AllDraw *TakeRoot::Node = nullptr;
AllDraw *TakeRoot::Pointer = nullptr;

	void TakeRoot::CalculateRootGray(AllDraw *Curent)

	{
	/*
	    try
	    {
	        if (Node != null)
	        {
	            if (Node.Dept != null)
	                Pointer.Dept = Curent;
	            else
	                Node.Dept = Curent;
	        }
	        else
	            Node = Curent;
	    }
	    catch (Exception tt)
	    {
	        return;
	    }*/
		return;
	}
}
